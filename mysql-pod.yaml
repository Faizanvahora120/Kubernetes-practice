apiVersion: v1
kind: Pod
metadata:
  name: "mysql-pod"
  namespace: mysql-ns
  labels:
    app: "mysql-pod"
spec:
  containers:
  - name: "mysql-pod"
    image: "mysql:latest"
    env:
    - name: DB_HOST
      valueFrom:
        configMapKeyRef:
          name: myssql-pass
          key: password